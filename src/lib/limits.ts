/** The free-tier school cap. There's no accounts/payments system yet — see waitlist.ts — so this
 * is enforced client-side only, same as everything else about a visitor's list right now. */
export const FREE_SCHOOL_LIMIT = 5
